apiVersion: apps/v1
kind: Deployment
metadata:
  name: market
  namespace: poke-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: market
  template:
    metadata:
      labels:
        app: market
    spec:
      containers:
        - name: market
          image: REPLACE_WITH_DOCKERFILE_DEV_PATH
          args:
            - "src.services.market:main"
          env:
            - name: ENTRYPOINT_MODULE
              value: "src.services.market:main"
---
apiVersion: v1
kind: Service
metadata:
  name: market
  namespace: poke-namespace
spec:
  selector:
    app: market
  ports:
    - protocol: TCP
      port: 80 # Replace with the appropriate port
      targetPort: 80 # Replace with the appropriate port
  type: ClusterIP
